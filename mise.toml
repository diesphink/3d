[env]
_.python.venv = { path = ".venv", create = true }
VSCODE_PROFILES = "common python"

[tools]
python = "3.12"
uv = "latest"

[tasks.install]
description = "Install dependencies"
alias = "i"
run = "uv pip install -r requirements.txt"
